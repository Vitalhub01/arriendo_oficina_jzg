<div class="mx-auto max-w-lg px-4 py-12">
  <h1 class="text-2xl font-bold">Recuperar contraseña</h1>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "mt-6 space-y-4" }) do |f| %>
    <div>
      <%= f.label :email, class: "block text-sm font-medium" %>
      <%= f.email_field :email, autofocus: true, class: "input-field" %>
    </div>
    <%= f.submit "Enviar instrucciones", class: "btn-primary w-full" %>
  <% end %>
  <%= link_to "Volver al login", new_user_session_path, class: "mt-4 inline-block text-sm text-rose-600 hover:underline" %>
</div>
