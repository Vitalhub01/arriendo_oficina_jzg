Hola <%= @invoice.user.name %>,

Adjuntamos la boleta electronica <%= @invoice.folio %> correspondiente a tu reserva en <%= @booking.space.title %>.

Fecha: <%= l(@booking.start_at.to_date, format: :long) %>
Horario: <%= l(@booking.start_at, format: "%H:%M") %> — <%= l(@booking.end_at, format: "%H:%M") %>
Total: <%= @booking.formatted_total %>

Gracias por usar VitalHub.
